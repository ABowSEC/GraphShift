import { Box, Heading, Text } from '@chakra-ui/react';

const Sources = () => (
  <Box p={8}>
    <Heading>Source Library</Heading>
    <Text mt={4} fontSize="lg">
      Browse our collection of official documents, databases, and archives.
    </Text>
  </Box>
);

export default Sources;
